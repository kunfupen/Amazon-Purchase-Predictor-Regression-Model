# Data

This folder is reserved for the source datasets used by the regression notebooks.

## Expected Files

- `train.csv`: Training data containing the response variable `log_total` and predictor variables.
- `test.csv`: Test data used for generating prediction outputs.

These files are not included in the current repository snapshot. Place them in this folder before rerunning the notebooks in [`../analysis`](../analysis).
